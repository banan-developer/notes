rows, err := db.Query(
		"SELECT id, content, user_id FROM notes WHERE user_id = 1",
	)

Query - отправляет sql запрос, возвращает строки, используется только для SELECT

rows - указатель на результат в базе 



for rows.Next() {
		var note Note
		rows.Scan(&note.ID, &note.Content, &note.UserID)
		notes = append(notes, note)
	}

проходимся по всем элемнтам, с помощью next и кидаем их в наш слайс, а потом этот слайс в формате json отправляется на фронт